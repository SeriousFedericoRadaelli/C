#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main(){


int array[] = {3, 4, 5, 6, 7, 8};

array[2] = 400; //cambio valore al 3 elemento 

printf("%d", array[0]); //accedi al elemento del array che selezioni 

int array2[30]; //array di cui non so gli elementi 
         array2[0] = 4; //li assegno ogni elemneto 
         array2[1] = 4;
 return 0;
}